<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <label class="navbar-brand">Node js Socket io Chat Application</label>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="nav nav-tabs">
        <li class="nav-item active">
            <a class="nav-link" href="/">Chat Público <span class="sr-only">(current)</span></a>
        </li>
        <% if(everyauth.loggedIn) { %>
            <li class="nav-item">
                <a class="nav-link" href="/chatPrivado">Chat Privado</a>
            </li>
        <% } %>
    </ul>
    <ul class="navbar-nav ml-auto">
        <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Login
            </a>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
            <% if(!everyauth.loggedIn) { %>
                <a class="nav-link" href="/login">Login</a>
                <a class="nav-link" href="/auth/facebook">Login Facebook</a>
                <a class="nav-link" href="/register">Registo</a>
            <% } else { %>
                <a class="dropdown-item" href="/logout">Logout</a>
                <% if(everyauth.user) { %> 
                <%    user = everyauth.user; %>
                <% } else if(everyauth.facebook) { %>
                <%    user = everyauth.facebook.user; %>
                <% } %>
            <% } %>
        </div>
        </li>
    </ul>
  </div>
</nav>